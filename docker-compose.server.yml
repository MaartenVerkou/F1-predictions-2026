services:
  app:
    ports: !reset []
    networks:
      default: {}
      web:
        aliases:
          - f1-app
      db: {}
  caddy:
    profiles: !override
      - legacy-edge
    networks:
      default: {}
      web: {}

networks:
  web:
    external: true
    name: wok-web
  db:
    external: true
    name: wok-db
