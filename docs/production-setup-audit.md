# Production Setup Audit

Generated by production-project-setup. This report is non-destructive: no files were deleted or rewritten by the audit.

## Existing Deployment Files
- Found Dockerfile
- Found .dockerignore
- Found docker-compose.yml
- docker-compose.yml uses local image builds. That is fine for server-side docker compose up --build; registry-based deploys usually switch services to an image: reference.
- docker-compose.yml references prebuilt images. Good fit for GitHub Actions building/pushing images to a registry.

## GitHub Actions
- Found workflow .github/workflows/ci.yml
- Found workflow .github/workflows/codeql.yml

## Package Scripts
- Has script: lint
- Has script: typecheck
- Has script: test
- Has script: build
- Has script: start
- Missing script: preview
- Has script: test:e2e

## Environment Files
- Found local env file: .env. Ensure it is ignored and not committed.

## Generated/Local Artifacts
- Found node_modules. Keep it ignored unless this repo intentionally commits that output.

## Follow-Up Applied

- Added a reproducible `package-lock.json` and aligned GitHub Actions with the production Docker runtime on Node 20.
- Added syntax/build checks, Node smoke tests, and Playwright browser smoke coverage.
- Added `GET /healthz`, Docker healthcheck, structured JSON request/error logs, and request IDs.
- Replaced the vulnerable `connect-sqlite3`/`sqlite3` session dependency with a `better-sqlite3` session store using the same `sessions` table shape.
- Updated `bcrypt`, `nodemailer`, and native SQLite dependency lines; `npm audit --audit-level=high` now passes.
- Confirmed the deployed server uses external Restic backups to Cloudflare R2; this was originally `/etc/cron.d/wok-backup` and is now managed through `/etc/cron.d/mhv-backup`. No repo-local backup script should supersede it without an explicit migration.

## Suggested Follow-Up Prompt

```text
I ran production-project-setup. Review docs/production-setup-audit.md and make this existing app work cleanly with the GitHub Actions, Docker, Playwright, security, and observability baseline. Do not remove existing deployment behavior unless you prove it is obsolete. Choose the right workflow for this request.
```
