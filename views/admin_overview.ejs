<%- include('partials/header', { title: 'Admin overview', user }) %>
<%- include('partials/admin_nav', { active: 'overview' }) %>

<div class="card">
  <h1>Admin overview</h1>
  <p class="muted">Accounts, groups, memberships, and responses.</p>
  <% if (adminError) { %>
    <div class="alert"><%= adminError %></div>
  <% } %>
  <% if (adminSuccess) { %>
    <p class="muted"><%= adminSuccess %></p>
  <% } %>

  <h2>Groups</h2>
  <table class="table compact">
    <thead>
      <tr>
        <th>Group</th>
        <th>Owner</th>
        <th>Created</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% groups.forEach(row => { %>
        <tr>
          <td><%= row.name %></td>
          <td><%= row.owner_name %></td>
          <td><%= new Date(row.created_at).toLocaleString() %></td>
          <td>
            <div class="table-actions">
              <a class="link" href="/groups/<%= row.id %>/leaderboard">Leaderboard</a>
              <form method="post" action="/admin/groups/<%= row.id %>/delete" onsubmit="return confirm('Delete this group and all related data?')">
                <button class="secondary" type="submit">Delete group</button>
              </form>
            </div>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <div class="inline-actions" style="margin-top: 10px;">
    <% if (currentGroupPage > 1) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage - 1 %>&usersPage=<%= currentUserPage %>&membershipsPage=<%= currentMembershipsPage %>&responsePage=<%= currentResponsePage %>">Previous <%= groupsPerPage %></a>
    <% } %>
    <span class="muted">Groups page <%= currentGroupPage %> of <%= totalGroupPages %></span>
    <% if (currentGroupPage < totalGroupPages) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage + 1 %>&usersPage=<%= currentUserPage %>&membershipsPage=<%= currentMembershipsPage %>&responsePage=<%= currentResponsePage %>">Next <%= groupsPerPage %></a>
    <% } %>
  </div>

  <h2 style="margin-top:24px;">Users</h2>
  <table class="table compact">
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Admin</th>
        <th>Created</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach(row => { %>
        <tr>
          <td><%= row.name %></td>
          <td><%= row.email %></td>
          <td><%= row.is_admin ? 'Yes' : 'No' %></td>
          <td><%= new Date(row.created_at).toLocaleString() %></td>
          <td>
            <div class="table-actions">
              <% if (row.is_admin) { %>
                <form method="post" action="/admin/users/<%= row.id %>/remove-admin">
                  <button class="secondary admin-toggle-btn" type="submit">Remove admin</button>
                </form>
              <% } else { %>
                <form method="post" action="/admin/users/<%= row.id %>/make-admin">
                  <button class="secondary admin-toggle-btn" type="submit">Make admin</button>
                </form>
              <% } %>
              <form method="post" action="/admin/users/<%= row.id %>/delete" onsubmit="return confirm('Delete this user and all their memberships/responses?')">
                <button class="secondary" type="submit">Delete user</button>
              </form>
            </div>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <div class="inline-actions" style="margin-top: 10px;">
    <% if (currentUserPage > 1) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage %>&usersPage=<%= currentUserPage - 1 %>&membershipsPage=<%= currentMembershipsPage %>&responsePage=<%= currentResponsePage %>">Previous <%= usersPerPage %></a>
    <% } %>
    <span class="muted">Users page <%= currentUserPage %> of <%= totalUserPages %></span>
    <% if (currentUserPage < totalUserPages) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage %>&usersPage=<%= currentUserPage + 1 %>&membershipsPage=<%= currentMembershipsPage %>&responsePage=<%= currentResponsePage %>">Next <%= usersPerPage %></a>
    <% } %>
  </div>

  <h2 style="margin-top:24px;">Memberships</h2>
  <table class="table compact">
    <thead>
      <tr>
        <th>User</th>
        <th>Group</th>
        <th>Role</th>
        <th>Joined</th>
      </tr>
    </thead>
    <tbody>
      <% memberships.forEach(row => { %>
        <tr>
          <td><%= row.user_name %></td>
          <td><%= row.group_name %></td>
          <td><%= row.role %></td>
          <td><%= new Date(row.joined_at).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <div class="inline-actions" style="margin-top: 10px;">
    <% if (currentMembershipsPage > 1) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage %>&usersPage=<%= currentUserPage %>&membershipsPage=<%= currentMembershipsPage - 1 %>&responsePage=<%= currentResponsePage %>">Previous <%= membershipsPerPage %></a>
    <% } %>
    <span class="muted">Memberships page <%= currentMembershipsPage %> of <%= totalMembershipPages %></span>
    <% if (currentMembershipsPage < totalMembershipPages) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage %>&usersPage=<%= currentUserPage %>&membershipsPage=<%= currentMembershipsPage + 1 %>&responsePage=<%= currentResponsePage %>">Next <%= membershipsPerPage %></a>
    <% } %>
  </div>

  <h2 style="margin-top:24px;">Responses</h2>
  <table class="table compact">
    <thead>
      <tr>
        <th>User</th>
        <th>Group</th>
        <th>Question</th>
        <th>Answer</th>
        <th>Updated</th>
      </tr>
    </thead>
    <tbody>
      <% responses.forEach(row => { %>
        <tr>
          <td><%= row.user_name %></td>
          <td><%= row.group_name %></td>
          <td><%= row.question_id %></td>
          <td><%= row.answer %></td>
          <td><%= new Date(row.updated_at).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  <div class="inline-actions" style="margin-top: 10px;">
    <% if (currentResponsePage > 1) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage %>&usersPage=<%= currentUserPage %>&membershipsPage=<%= currentMembershipsPage %>&responsePage=<%= currentResponsePage - 1 %>">Previous <%= responsesPerPage %></a>
    <% } %>
    <span class="muted">Page <%= currentResponsePage %> of <%= totalResponsePages %></span>
    <% if (currentResponsePage < totalResponsePages) { %>
      <a class="link" href="/admin/overview?groupPage=<%= currentGroupPage %>&usersPage=<%= currentUserPage %>&membershipsPage=<%= currentMembershipsPage %>&responsePage=<%= currentResponsePage + 1 %>">Next <%= responsesPerPage %></a>
    <% } %>
  </div>
</div>

<%- include('partials/footer') %>

