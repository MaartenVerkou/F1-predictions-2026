<%- include('partials/header', { title: 'Forgot password', user: null }) %>

<div class="card" style="max-width:480px; margin: 0 auto;">
  <h1>Reset your password</h1>
  <p class="muted">Enter your account email and we will send a reset link.</p>
  <% if (error) { %>
    <div class="alert"><%= error %></div>
  <% } %>
  <% if (message) { %>
    <p class="muted"><%= message %></p>
  <% } %>
  <% if (debugResetUrl) { %>
    <p class="muted"><strong>Debug reset link:</strong> <a class="link underlined-link" href="<%= debugResetUrl %>"><%= debugResetUrl %></a></p>
  <% } %>
  <form method="post" action="/forgot-password">
    <label>
      Email
      <input type="email" name="email" required />
    </label>
    <button type="submit">Send reset link</button>
  </form>
</div>

<%- include('partials/footer') %>
