<%- include('partials/header', { title: t('header.site_title'), user }) %>

<section class="hero">
  <div class="card">
    <span class="badge home-how-it-works-badge"><%= t('home.badge_how_it_works') %></span>
    <h1><%= t('home.headline') %></h1>
    <p class="muted"><%= t('home.subtitle') %></p>
    <div class="inline-actions">
      <% if (!user) { %>
        <a class="button-link" href="/signup"><%= t('home.create_account') %></a>
      <% } else { %>
        <a class="button-link hero-start-predicting" href="/start-predicting"><%= t('home.start_predicting') %></a>
        <a class="link" href="/dashboard"><%= t('home.go_dashboard') %></a>
      <% } %>
      <a class="link" href="/about"><%= t('home.about_link') %></a>
      <% if (paypalDonationUrl) { %>
        <a class="button-link" href="<%= paypalDonationUrl %>" target="_blank" rel="noopener noreferrer"><%= paypalDonationLabel %></a>
      <% } %>
    </div>
  </div>

  <div class="card">
    <h2><%= t('home.flow_title') %></h2>
    <ol>
      <li><%= t('home.flow_step_1') %></li>
      <li><%= t('home.flow_step_2') %></li>
      <li><%= t('home.flow_step_3') %></li>
    </ol>
  </div>
</section>

<%- include('partials/footer') %>
